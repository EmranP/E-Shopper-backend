import { Router } from 'express'

// !Todo: Make Orders, Order_items, Cart, Cart_item from db and Make logger file for show result logger


const routerCarts = Router({mergeParams: true})

export default routerCarts